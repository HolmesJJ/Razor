// 编写 MessageBox 组件
import MessageBox from './src/main.js';

/* istanbul ignore next */
// eslint-disable-next-line
MessageBox.install = function(Vue) {
  // Vue.component('Rz' + MessageBox.options.name, MessageBox);
};

export default MessageBox;
